# Login System (DAL/BLL/UI Practice)

A simple 3-layer login system built with Python and SQLite, separating concerns into:
- `adal.py` — Data Access Layer (talks to the database)
- `bll.py` — Business Logic Layer (validates input, applies rules)
- `aui.py` — UI Layer (takes user input, shows result)

